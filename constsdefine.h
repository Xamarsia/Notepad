#pragma once

#include <QString>

namespace notepadConstants {

const QString untitled = "Untitled";

const QString crlfLineEnding = "Windows (CRLF)";

const QString lfLineEnding = "Linux (LF)";

const QString crLineEnding = "MacOS (CR)";

const QString cannotFind = "Cannot find";

const QString programName = "Notepad";

const QString organizationName = "MySoft";

const QString directoryPathKey = "directoryPath";

const QString encodindLabel = "Encoding:";

const QString openFileDialogTitle = "Open a file";

const QString saveAsDialogTitle = "Save As";

const QString findDialogTitle = "Find";

const QString aboutNotepadDialogTitle = "About Notepad";

const QString replaceDialogTitle = "Replace";

const QString autoDetectEncoding = "Auto-Detect";

const QString systemEncoding = "System";

const QString UTF16LEEncoding = "UTF-16LE";

const QString UTF16BEEncoding = "UTF-16BE";

const QString UTF8Encoding = "UTF-8";

const QString UTF8BOMEncoding = "UTF-8 with BOM";

const QString textFileDialogDocumentsFilter = "Text Documents (*.txt)";

const QString allFilesDialogFilter = "All Files(*.*)";

const QString txtFileExtention = ".txt";

const QString documentModifiedMessage = "The document has been modified.\nDo you want to save your changes?";

const int defaultZoom = 100;

const int minZoom = 10;

const int maxZoom = 500;

const int stepSizeForZoom = 10;

}
